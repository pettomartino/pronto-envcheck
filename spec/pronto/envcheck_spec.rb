require "spec_helper"

RSpec.describe Pronto::Envcheck do
  it "has a version number" do
    expect(Pronto::Envcheck::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
